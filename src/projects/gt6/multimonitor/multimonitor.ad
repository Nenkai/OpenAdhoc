








module MultiMonitorProject
{
    import main::SequenceUtil;

    function onLoad(update_context)
    {
        update_context.createRenderContext(1);
        var render_context = update_context.getRenderContext(0);
        render_context.pushPage(TopRoot);
    }
}