RootWindow {
  name string{"SceneFaceRoot"}
  has_script digit{1}
  children[1] {
    SceneFace {
      name string{"SceneFace"}
    }
  }
  noload digit{1}
  react_event digit{0}
  manual_close digit{1}
}