












module IzumiRoot
{
    method onInitialize(context)
    {
        var scene_face = KinGinDou::SceneFace;
        scene_face.setClearColorBuffer(false);
        var scene = "licenseTrophyGold";
        SceneFaceUtil::loadScene(scene_face, scene);
        SceneFaceUtil::SetSignalHandler(scene_face, "start");
    }
}

