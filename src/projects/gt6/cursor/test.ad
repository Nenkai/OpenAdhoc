top_level: for(;;)
{
    label: for (var i = 0; i < 5; i++)
    {
        for (var j = 0; j < 5; j++)
        {
            if (true)
                break top_level;
            else
            {
                pr("hello before regular break");
                break;
            }
        }

        pr("hello world");
    }
    pr("top level");
}